package com.zilliz.milvus.client

/** MilvusClient：DDL、describe、Delete、快照、索引、load、release、flush、compact、BatchUpdateManifest、RegisterSegments（待 Milvus 提供）；proto 的 DataType 与 core 的 MilvusType 互转。
  *
  * 承载的功能：C1、C2、W5、A1、A2、A3、A4、A5、A6（见 docs/design/capabilities.md）。
  */
package object api

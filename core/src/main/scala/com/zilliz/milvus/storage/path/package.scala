package com.zilliz.milvus.storage

/** Normalizes the three path shapes the format uses into (bucket, key).
  *
  * Main types: StoragePath, Located. Capabilities: R3 (see
  * docs/design/capabilities.md).
  */
package object path

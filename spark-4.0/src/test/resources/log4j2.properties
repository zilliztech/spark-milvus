# Log4j 2.x configuration for tests

# Root logger level
rootLogger.level = info
rootLogger.appenderRef.stdout.ref = ConsoleAppender

# Console appender configuration
appender.console.type = Console
appender.console.name = ConsoleAppender
# Standard out, so a reader can tell a log line from a failure: sbt labels
# everything a forked JVM writes to standard error as an error, and these are
# mostly INFO.
appender.console.target = SYSTEM_OUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n

logger.milvus.name = com.zilliz.spark.connector
logger.milvus.level = info
logger.milvus.additivity = false
logger.milvus.appenderRef.stdout.ref = ConsoleAppender

# The layers below Spark log under com.zilliz.milvus. Without a logger of their
# own they fall to the root, which a harness calling setLogLevel("WARN") turns
# down, and every core line disappears -- including the ones a search writes
# about the segment set it holds.
logger.core.name = com.zilliz.milvus
logger.core.level = info
logger.core.additivity = false
logger.core.appenderRef.stdout.ref = ConsoleAppender

# Reduce Spark noise - only WARN and above
logger.spark.name = org.apache.spark
logger.spark.level = warn

logger.sparkproject.name = org.sparkproject
logger.sparkproject.level = warn

logger.hadoop.name = org.apache.hadoop
logger.hadoop.level = warn

logger.parquet.name = org.apache.parquet
logger.parquet.level = warn

logger.parquet2.name = parquet
logger.parquet2.level = warn

# Reduce other verbose libraries
logger.jetty.name = org.eclipse.jetty
logger.jetty.level = warn

logger.netty.name = io.netty
logger.netty.level = warn

logger.arrow.name = org.apache.arrow
logger.arrow.level = warn
